/**
 * Hier finden sich Basisklassen als Grundlage fuer die Test-Beispiele.
 * 
 * <h2>Warum befinden sich diese im Test-Zweig (src/test/java) und nicht im Main-Zweig (src/main/java)?</h2>
 * Weil hier nur Beispiele für die Tests sind, die nicht direkt verwendet werden koennen.
 * Die wirklich nuetzlich erscheinenden Klassen finden sich im Main-Zweig, wie dem CheckAppender,
 * oder einer entsprechenden Rule oder speziellen Matcher.
 */
package hegmanns.it.de.junit.basisklassen;